// var

var alturaV = 5;
var comprimentoV = 7;

areaV = alturaV * comprimentoV;
console.log(areaV)
var areaV;

// let

let forma = 'retangulo';
let alturaL = 6;
let comprimentoL = 5;

if (forma === 'retangulo'){
    areaL = alturaL * comprimentoL
} else {
    areaL = (alturaL * comprimentoL) / 2;
}

console.log(areaL)

// const

const formaC = 'triangulo';
const alturaC = 6;
const comprimentoC = 5;
let area;

if (formaC === 'retangulo') {
    area = alturaC * comprimentoC
} else {
    area = (alturaC * comprimentoC) / 2;
}

console.log(area)
